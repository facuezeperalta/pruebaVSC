// setTimeout(function, tiempo en milisegundos que quiero);

var texto = 'Hola mundo';

setTimeout(saludar,5000); //ejecuta la funcion saludar luego de 5 segundos.

function saludar(){
    console.log(texto);
}
